# Required — get a free key at https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here

# Optional — all of these have sensible defaults (see src/config.py).
# Uncomment and change only if you want to override a default.

# RESUME_AI_MODEL=llama-3.3-70b-versatile
# LLM_REQUEST_TIMEOUT_SECONDS=30
# LLM_MAX_RETRIES=2
# SEMANTIC_MATCH_THRESHOLD=0.6
# TFIDF_SCORE_WEIGHT=0.3
# SEMANTIC_SCORE_WEIGHT=0.7
# EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2
# MAX_UPLOAD_SIZE_MB=10
# MIN_RESUME_TEXT_LENGTH=50
# LOG_LEVEL=INFO
